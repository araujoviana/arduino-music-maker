#define BUZZER_PIN 9 // Must be PWM capable (~) and must match F# code
#define BAUD_RATE 9600 // Must match F# code
#define BASE_UNIT 1000 // Base unit for note duration
#define TONE_PAUSE 50 // Predefined pause between each note